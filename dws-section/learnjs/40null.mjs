let a
console.log(a);

a = 3
console.log(3);

a = null
console.log(null);

// What is undefined and null ?

// undefine is variable is defined but not initialized also value is empty too(undefined).

// null is variable is defined and initialized but value empty(null).